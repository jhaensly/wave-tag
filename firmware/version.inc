APP_VERSION_MAJOR := 0
APP_VERSION_MINOR := 1
APP_VERSION_SUB   := 'a'

DEFINES += -DAPP_VERSION_MAJOR=$(APP_VERSION_MAJOR)
DEFINES += -DAPP_VERSION_MINOR=$(APP_VERSION_MINOR)
DEFINES += -DAPP_VERSION_SUB=$(APP_VERSION_SUB)

VERSON_GIT_HASH := $(shell git rev-parse HEAD | head -c 6)
DEFINES += -DVERSION_GIT_HASH=$(VERSION_GIT_HASH)
